import 'package:flutter/material.dart';

class PageScreen3 extends StatelessWidget {
  const PageScreen3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Center(child: const Text('Page 3')),
    );
  }
}
